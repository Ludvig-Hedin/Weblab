{
    "name": "@weblab/web",
    "module": "index.ts",
    "type": "module",
    "private": true,
    "scripts": {
        "dev": "bun --filter '@weblab/web-client' --filter '@weblab/web-preload' dev",
        "dev:client": "bun --filter @weblab/web-client dev",
        "dev:server": "bun --filter @weblab/web-server dev",
        "dev:preload": "bun --filter @weblab/web-preload dev",
        "build:client": "bun --filter @weblab/web-client build",
        "start:client": "bun --filter @weblab/web-client start",
        "docker:up": "docker compose up",
        "docker:build": "docker compose build",
        "docker:down": "docker compose down",
        "docker:restart": "docker compose down && docker compose up"
    },
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "dependencies": {}
}

// DEPRECATED: this router was never mounted in `root.ts` and duplicates the
// server-side `compressImageServer` use in `routers/project/project.ts`. It is
// retained as an empty module so external imports of the path do not break,
// but it intentionally exports nothing reachable.
export {};

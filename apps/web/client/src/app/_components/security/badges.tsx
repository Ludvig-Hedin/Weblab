// Deprecated: the badges section is now folded into SecurityDataFeatures via the
// shadcn `Compliance1` block. This file is retained only because the dev sandbox
// blocked its deletion in this session; safe to remove on next chore-pass.
export {};
